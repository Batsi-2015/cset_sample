# Core pipeline
requests>=2.31
pyyaml>=6.0
pandas>=2.0
numpy>=1.24
duckdb>=1.0
rapidfuzz>=3.0
scikit-learn>=1.3

# Topic modeling (optional but recommended; the pipeline falls back to TF-IDF without them)
sentence-transformers>=2.7
umap-learn>=0.5

# LLM annotation (optional)
anthropic>=0.40

# Report
matplotlib>=3.7
jupyter>=1.0
tabulate>=0.9
